services:
  rsa:
    image: node:22
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
      - "5173:5173"
    command: sh -c "npm install && npm run dev"
    env_file:
      - .env
